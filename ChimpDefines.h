#ifndef CHIMPDEFINES_H
#define CHIMPDEFINES_H

namespace chimp
{

enum Layer { BACK, MID, FORE };
enum Faction { FACTION_VOID = 0, FACTION_PLAYER = 1<<0, FACTION_BADDIES = 1<<1 };

} // namespace chimp

#endif // CHIMPDEFINES_H
